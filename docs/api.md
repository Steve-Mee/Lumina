# API Reference

## BibleEngine

- `BibleEngine(file_path)`
- `load()`
- `save()`
- `evolve(updates)`
- `export_public_bible()`

## Workflow Functions

- `reflect_on_trade(app, pnl_dollars, entry_price, exit_price, position_qty)`
- `process_user_feedback(app, feedback_text, trade_data=None)`
- `dna_rewrite_daemon(app, interval_seconds=900)`

## VectorContributionAPI

- `upload_experience(collection, context, metadata)`
- `upload_batch(collection, rows)`
