# Development, test, and lint tooling — NOT needed at runtime
# Install with: pip install -r requirements-dev.txt

# -- Test runner & plugins --------------------------------------------------
pytest==9.0.2
pytest-cov==7.1.0
pytest-timeout==2.4.0       # Global timeout per test (addopts = --timeout=30)
pytest-xdist==3.7.0         # Parallel test execution: pytest -n auto
pytest-randomly==3.16.0     # Deterministic but shuffled test order per seed

# -- Static analysis --------------------------------------------------------
mypy==1.15.0
pyright==1.1.400
ruff==0.11.6

# -- Security & dependency audit -------------------------------------------
pip-audit==2.9.0
safety==3.7.0
cyclonedx-bom==7.3.0        # SBOM generation (requires packaging>=25; aligns with requirements-core)

# -- Coverage ---------------------------------------------------------------
coverage==7.13.5
