# ML training & inference stack — required for PPO training, neuroevolution,
# and local model inference. NOT needed for paper/live execution without training.
#
# Install after core: pip install -r requirements-core.txt -r requirements-ml.txt

# -- Core ML frameworks ----------------------------------------------------
torch==2.11.0
numpy==2.3.5                # already in core, repeated for explicit pin
scipy==1.17.1               # already in core

# -- Reinforcement learning ------------------------------------------------
stable_baselines3==2.8.0
gymnasium==1.2.3
Farama-Notifications==0.0.4

# -- LLM inference & fine-tuning -------------------------------------------
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==0.36.2
sentencepiece==0.2.1
tiktoken==0.12.0
mistral_common==1.11.0
gguf==0.18.0
einops==0.8.2

# -- vLLM (local high-perf serving) ----------------------------------------
vllm==0.19.0
lm-format-enforcer==0.11.3
outlines_core==0.2.11
lark==1.2.2
interegular==0.3.3
partial-json-parser==0.2.1.1.post7
compressed-tensors==0.14.0.1
depyf==0.20.0

# -- ONNX runtime ----------------------------------------------------------
onnxruntime==1.24.4
flatbuffers==25.12.19
protobuf==6.33.6
googleapis-common-protos==1.74.0
grpcio==1.80.0

# -- Computer vision (chart screenshots) -----------------------------------
opencv-python-headless==4.13.0.92

# -- Sentence embeddings (community vector search) -------------------------
sentence-transformers==5.3.0

# -- Parallel compute (Ray) ------------------------------------------------
ray==2.54.1

# -- Symbolic math ---------------------------------------------------------
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1

# -- Voice I/O (optional, headless-safe via lazy import) -------------------
SpeechRecognition==3.15.2
pyttsx3==2.99

# -- Other ML utilities ----------------------------------------------------
threadpoolctl==3.6.0
toolz==1.1.0
pybase64==1.4.3
astor==0.8.1
ninja==1.13.0
