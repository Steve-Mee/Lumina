# Trading/live execution dependencies — install for broker-facing and market-aware runtime.
#
# Install after core: pip install -r requirements-core.txt -r requirements-trading.txt

# -- RL environment (imported at engine bootstrap; required for unit tests) --
gymnasium==1.2.3

# -- Trading schedules & market sessions ------------------------------------
exchange_calendars==4.13.2
pandas_market_calendars==5.3.2
APScheduler==3.11.1

# -- Broker/exchange connectivity -------------------------------------------
python-telegram-bot==22.5

# -- Execution Fabric (gRPC client to NT8 Fabric server) --------------------
# protobuf must stay <7 for xai-sdk / opentelemetry / streamlit compatibility.
# grpcio-tools 1.73.x requires protobuf>=6.30; 6.33.5+ clears PYSEC-2026-1805.
grpcio==1.73.1
protobuf==6.33.6
