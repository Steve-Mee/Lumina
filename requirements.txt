# Aggregated install for local development convenience.
# This file intentionally composes the split requirement sets.
# Production images should install only the needed subsets.
-r requirements-core.txt
-r requirements-trading.txt
-r requirements-ml.txt
-r requirements-dev.txt
