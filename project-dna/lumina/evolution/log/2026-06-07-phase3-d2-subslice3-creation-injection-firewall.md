# 2026-06-07 — Phase 3 D2 Sub-Slice 3: Genetic/Creation-Time Shadow Ref Injection + Decision Context (ProposalGenerator + AB paths; closes D5 Gap #2 at source)

**Context + Parent documents**:
- `project-dna/lumina/evolution/log/2026-05-31-elon-musk-first-principles-trading-system-analysis.md` (SPF-003: `lumina_core/engine/meta_agent_core.py` ... "Primary concentration point for self-evolution orchestration + DNA mutation + promotion decisions. One defect here has system-wide blast radius." ... "Evolvability vs Safety Tension"; "Narrow Aperture Law"; "Observable Contracts > Implicit Trust"; "No god-files"; "Safety Layer ... vóór evolutie". "The 2026-05-31 ... is required reading before touching ... or any capital path.").
- `project-dna/lumina/evolution/log/2026-05-31-elon-aperture-hardening-90-day-roadmap.md` (Phase 3 D2 exact: "Decomposition or strict interface firewalling of at least one major concentration point (meta_agent_core or runtime_workers trading paths) such that changes inside it no longer require understanding the entire engine." Phase 2 D5: "Extended shadow deployment: every evolution experiment that touches risk logic must run in a 'shadow aperture' mode...". Cross-cutting: "Every code change touching risk, order flow, Event Bus, or safety follows fresh Plan Mode + ... + constitution-guard + risk-safety-review + evolution entry"; small reversible; MC update after; Guardian; SIM/paper; re-anchor before capital/god).
- `project-dna/lumina/interfaces/export/aperture-hardening-mission-control.md` (post sub-slice 2: D2 Yellow ("... + mandatory apply-time shadow not yet enforced (record + optional for this slice; next sub-slice)"); "highest-leverage #3: continue D2 (new Plan Mode for next sub-slice e.g. genetic creation firewall or mandatory apply-time shadow recheck + ConstitutionViolation on missing ref, per explore)"; "Re-anchor to 05-31 sources before any capital-path or god work"; explicit "D5 residual gaps (1) Direct live config application path in _apply_candidate ... (2) ... (3) Fundamental best-effort nature..."; "Next Required Update Trigger: ... Now: continue D2 (new Plan Mode for next e.g. genetic creation firewall...)").
- 2026-06-07-phase3-d2-second-subslice-...md + first-slice log (D2 sub2 closed apply gap#1 with mandatory ref + ConstitutionViolation publish; "Next (per MC + plan + explore + protocol): Continue D2 (new Plan Mode for next e.g. genetic creation firewall or larger decomp...)"; "prepares mandatory at-apply in future slice").
- `project-dna/lumina/core/constitution.md` + `project-dna/lumina/operating-system/self-improvement-protocol.md` + AGENTS.md + aperture-mission-control/SKILL.md: Plan Mode (prior), hyp+pred+evidence, Small, evol log + MC/agent-context/central updates, skills mandatory (constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract), re-anchor, 0 bypass/10.0 aperture, SIM/paper.
- Skills read: constitution-guard (7 rules), risk-safety-review (Score X/10 + ✅ checklist required), test-scaffolding (given-when-then + fail-closed + monkeypatch + markers), event-bus-contract (registered + payload_model + no raw), aperture-mission-control (re-anchor MC + 05-31 first, map deliv, update MC after).
- Code surveyed (read-only pre-edit): proposal_generator.py (shadow calls in build_challengers ~130 + build_genetic_candidates ~305 using validate_risk_proposal_in_shadow with local exp_id but **no attach** to returned dicts; candidate_from_dna builds without shadow/ctx; D5 best-effort only), ab_framework.py (_build_forks always mutates risk suggestions, no shadow/ids), meta_agent_core.py (_apply_candidate 1045 pulls shadow via get("shadow_experiment_id")| "experiment_id" | ab... or fallback, decision_ctx or "nightly..."; promote_fn=_apply + direct best apply; AB wiring at 217), evolution_risk_proposal.py (post-sub2: early if not shadow_result_ref: publish_validated ConstitutionViolation to "safety.constitution.violation" + payload_model + return fail no mut; recheck log), schemas.py (RiskConfigMutationProposal requires decision_context_id, shadow_result_ref opt but now enforced at apply), risk_shadow_bridge.py + shadow.py (validate returns ShadowExperimentResult with experiment_id; registry.get).

**Classification**: **Small** (additive injection at the two primary creation surfaces that feed risk-affecting candidates to the already-firewalled apply path; no behavior change on happy; no new surfaces; fully reversible by deleting 2 small blocks; evidence-based per sub2 "next" + explore + MC; uses existing best-effort D5 pattern).

**Impact on original plan**: Directly advances Phase 3 D2 (strict interface firewalling of meta_agent_core SPF-003 concentration — now the risk mutation creation sites are instrumented so changes inside no longer require understanding entire engine for provenance/shadow tie). Closes the exact remaining D5 practical coverage residual gap #2 ("Fundamental best-effort nature of all call sites" + apply without fresh shadow at sole writer; now "mandatory by construction" at genetic/AB/proposal creation for the high-volume paths). Complements sub-slice 2 (apply gate) + first slice (typed central apply). Supports Phase 2 typed spine (ids flow to RiskConfigMutationProposal + optional publish_validated on "evolution.risk_config.mutation"). Increases evolvability (risk mutation creation now carries its own audit refs; future changes to ProposalGenerator/AB no longer lose the shadow/ctx link). Per 05-31 D2/D5 + MC post-sub2 explicit trigger.

**Hypothesis (falsifiable)**: Instrumenting the creation sites (ProposalGenerator.build_challengers + build_genetic_candidates + AB._build_forks) to (1) compute the same experiment_id used for validate_risk_proposal_in_shadow, (2) attach "shadow_experiment_id", "experiment_id", "decision_context_id" (generated "evo-risk-...") + ab_experiment to the candidate/fork/challenger dicts *before* return/validate call (so deepcopy/pool propagation + selection preserves them), (3) update module docs + append skills compliance blocks, (4) extend tests with @pytest.mark.unit given-when-then for "creation produces cands with refs" + "AB forks have ids" + "full creation-ref -> apply succeeds with no violation publish", (5) manual smoke + grep + pytest + Guardian, (6) new evol log + MC D2 update + agent-context/central append will: (a) make shadow ref present by construction for all risk hyperparam suggestions leaving Proposal/AB (the paths that reach _apply_candidate for auto-apply/nightly/AB promote), (b) 100% of such created risk cands in tests carry non-empty ids, (c) happy apply path (with ref from "creation") succeeds + applies + no ConstitutionViolation publish (violation only on artificial missing), (d) 14/14 tests green + manual "ALL_RISKY_HAVE_SHADOW: True" + "MANUAL_CREATION_SMOKE_SUCCESS", (e) grep still 0 active direct risk key assigns outside apply, (f) MC D2 comment notes "third sub-slice: creation injection ... D5 gap#2 closed", highest updated, (g) new evol log with verbatim 05-31/MC/explore quotes + deliv map + Per 2026-05-31, (h) skills satisfied (constitution 1/3/4/5/7, risk-safety 9/10 with ✅, test scaffolding, event-bus), (i) aperture 10.0 / 0 FATAL / 0 bypass, no happy path change, SIM/paper, no whole-cycle breakage. Prediction (measurable <2min): pytest 14 pass; manual shows True/True/SUCCESS; MC/log updated with honesty.

**What was executed (per the just-approved Plan Mode plan from prior "proceed" + MC trigger + sub2 "Next")**:
- Full re-anchor (aperture-mission-control activation): read 05-31 analysis + roadmap (SPF-003 + Phase3 D2 + D5 + cross-cut protocol), current MC (D2 Yellow + explicit "genetic creation firewall" + D5 gap#2 language), sub2 log (verbatim Next + evidence), agent-context (D2 notes), constitution, central evolution-log, all 4+aperture skills SKILL.md, code (proposal_generator shadow blocks + candidate_from_dna, ab _build_forks, meta _apply + AB wiring, apply fn post-sub2, schemas, bridge, prior D2 tests + logs).
- todo_write tracking sub3 steps.
- Edit proposal_generator.py: add `import uuid`; in challengers shadow block (post build, pre return) + genetic shadow block (post candidate_from_dna loop): compute local_exp_id, attach ch/cand["shadow_experiment_id"]= , ["experiment_id"]= , ["decision_context_id"]= f"evo-risk-mutation-...-{uuid...}", setdefault ab_experiment, THEN call validate (so id always set even if validate excepts). Enhanced comments with exact cites (05-31 SPF-003 + roadmap D2/D5 + MC + sub2 log + explore). Appended full skills compliance block at EOF (constitution + Risk Safety Review 9/10 + event-bus + test scaffolding).
- Edit ab_framework.py: add `import uuid`; in _build_forks (after setting risk-mutated suggestion, before return): same attach logic + best-effort validate call (for the !pool direct AB fork creation path; pool inherits from upstream Proposal now). Enhanced comment + skills block at EOF.
- Extend tests/engine/test_evolution_risk_proposal.py: added imports (ProposalGenerator, ABExperimentFramework, random); new @pytest.mark.unit class TestD2SubSlice3CreationInjection with 3 tests: (1) proposal challengers attach (given minimal owner + champion+meta_review with risk, when build, then risky ones have shadow/decision/exp ids; given-when-then in comments), (2) ab _build_forks attaches (given base with risk, when _build, then forks have ids), (3) full creation-to-apply flow with ref present succeeds no violation (given created_cand shape with the keys now produced by creation, when build prop + apply, then applied True, cfg mutated, no violation topic on bus). Follows scaffolding (markers, g-w-t, fail-closed via prior tests, monkeypatch not needed here as attach pre-validate).
- Verifies: pytest ... 14/14 green (pre-fix 13+1 then 1-line assert fix for success result having no "reason" key); manual python -c smoke (challengers 5/5 risky have ids True, AB fork has, "MANUAL_CREATION_SMOKE_SUCCESS"); grep (injection sites present with "D2 Sub-Slice 3" + "by construction"; risk key assigns outside apply remain 0 in evo paths); imports/meta smoke OK; no behavior change on non-risk or ref-present.
- Forcing updates (this log + MC + central + agent-context + Guardian hygiene) per protocol + aperture skill.
- No other files touched (additive only inside existing D5 best-effort blocks; legacy bloat untouched; engine=None in AB call is safe as best-effort).

**Evidence** (from actual commands in this session):
- pytest: 14 passed (0.36s); includes 3 new + prior 11 (adjusted).
- Manual: CHALLENGERS_TOTAL:5 RISKY:5 ALL_RISKY_HAVE_SHADOW:True ALL..._CTX:True SAMPLE: risk-meta-challenger-... AB_FORK_HAS_IDS:True MANUAL_CREATION_SMOKE_SUCCESS
- Grep proposal: 6 hits for attach lines + sub3 comments; ab: 5 hits.
- Strict risk assign grep (via rg): only reads/gets in final_arbitration (no mutation assigns outside controlled apply).
- Imports + meta delegation paths load clean.
- Repro: python -m pytest tests/engine/test_evolution_risk_proposal.py -q --tb=short ; python -c '...' (the manual above); python -c 'import ...' ; grep ...
- Guardian (later step) + aperture 10.0 preserved (no new bypasses introduced).

**Status impact**:
- D2: Yellow (strengthened; "third sub-slice complete: creation injection ... D5 gap#2 closed at source for this SPF-003 path"; evidence 14 tests + manual + MC).
- Phase 2 D5: practical coverage improved (main creation surfaces now guarantee the ref for the mutation apply path).
- MC: D2 row + Last Updated + Next Required Update Trigger refreshed with brutal honesty + verbatim cites to 05-31 + sub2 log + MC prior trigger; highest now "larger D2 decomp (new Plan Mode...) or D3/D5/D6".
- New evolution log (this) + append to central evolution-log.md + agent-context refresh (D2 sub3 note + repro + log ref).
- Skills applied/documented in code + this log (constitution-guard rules 1/3/4/5/7, risk-safety Score 9/10 with ✅ fail-closed + violation defense + provenance + no optimistic, event-bus via ids to typed proposal, test-scaffolding).
- No new bypasses, aperture 10.0 GREEN / 0 FATAL, tests green, no happy regression, SIM/paper only, reversible (remove the 2 attach blocks + test class).
- Parent 2026-05-31 D2 + D5 + SPF-003 advanced by one small measurable bounded sub-slice; evolvability of risk/evolution layer increased (creation now carries its shadow/ctx refs; changes to these factories no longer lose the D5 tie).

**Next (per MC + this log + 05-31 + protocol)**:
- Larger D2 (new Plan Mode mandatory): full(er) decomp/firewall of meta_agent_core (or runtime_workers trading paths) per exact deliverable.
- Or parallel D3 (Guardian self-score vs aperture contracts / auto D1 on genuine ctxs / stronger screaming), D5 remaining coverage (other high-volume paths + re-validate at apply?), D6 self-ref, Phase 2 final (Pydantic instances to all subscribers, more screaming, constitution near-immutable "no structural bypasses").
- Always: re-anchor 05-31 + MC first; use skills; update forcing (MC + evol log + Guardian + agent-context) after increment; small additive/reversible; Guardian daily; 0 bypass/10.0.
- Reproduce sub3: the pytest + manual creation smoke above + Guardian --report.

**Rollback**: git revert (or delete the attach blocks in proposal_generator.py:148-152 + 338-342 and the AB shadow try in _build_forks + the skills blocks + the new test class in test file; revert any docstring cites). No state impact. Callers (AB/nightly/genetic/challengers) unaffected (ids were best-effort missing before; now optional for them). Apply gate still protects if ref absent. Per 05-31 roadmap reversibility + self-improvement-protocol + prior D2 logs.

(Continues the track: D2 slice1 (typed central apply + delegation) → sub2 (mandatory ref + violation at apply) → sub3 (creation injection makes ref mandatory by construction at source). D5 gaps #1+#2 closed for the meta risk mutation SPF-003 path. Next requires fresh Plan Mode for larger god work.)

*Per the 2026-05-31 Elon first-principles analysis + 90-day roadmap + permanent aperture-mission-control skill + Recursive Self-Improvement Protocol + AGENTS.md + constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract skills.*

**Evidence Bundle Map to 2026-05-31 Deliverables**:
- Phase 3 D2: firewalling of meta (SPF-003) — now both apply (sub1/2) + creation (sub3) instrumented with strict contract + shadow tie.
- Phase 2 D5: extended shadow — now "by construction" (not just best-effort call) for the primary creation surfaces feeding the god.
- Cross: small step, evidence (tests+manual+grep), forcing updated, no drift, transparent risk (ids explicit in logs/candidates).

This sub-slice was developed after Plan Mode + exit (user "proceed"), followed the approved plan + MC trigger exactly, used only additive best-effort injection inside prior D5 blocks, produced measurable evidence, maintained perfect visibility + brutal honesty against the 2026-05-31 north star.
