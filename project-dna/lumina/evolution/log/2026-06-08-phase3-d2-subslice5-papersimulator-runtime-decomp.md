# 2026-06-08 — Phase 3 D2 Sub-Slice 5: Bounded PaperSimulator for paper sim state + execution (further firewall/decomp of runtime_workers trading paths god; thin delegation from supervisor_inner; advances "or runtime_workers" + MC "full supervisor decomp in runtime_workers")

**Context + Parent documents**:
- `project-dna/lumina/evolution/log/2026-05-31-elon-musk-first-principles-trading-system-analysis.md` (SPF-006 verbatim: "Multiple Order construction sites with inconsistent metadata (runtime_workers.py:297,1197,1272 + ... ) 4+ sites construct `Order(...)` directly. Some paths (EOD force close, paper sim workers) pass incomplete proposed_risk / regime / confluence."; god-component: runtime_workers 74.7 KB "contains EOD force close, paper simulation trading loops, supervisor logic. High surface area for mode-specific bugs."; Phase 3 D2 deliverable: "Decomposition or strict interface firewalling of at least one major concentration point (meta_agent_core or runtime_workers trading paths) such that changes inside it no longer require understanding the entire engine."; "Narrow Aperture Law"; "Truth Density of Provenance"; "required reading before touching order flow/risk/safety/gatekeeper/Event Bus/any capital path").
- `project-dna/lumina/evolution/log/2026-05-31-elon-aperture-hardening-90-day-roadmap.md` (Phase 3 D2 exact: "Decomposition or strict interface firewalling of at least one major concentration point (meta_agent_core or runtime_workers trading paths) such that changes inside it no longer require understanding the entire engine."; Phase 2 deliv 2/4: 100% ... order submissions published as typed events with full lineage (`decision_context_id` + `prev_hash` chaining); cryptographic provenance; cross-cutting: fresh Plan Mode + constitution-guard + risk-safety-review + evolution entry; small reversible; MC update after; Guardian; SIM/paper; re-anchor before capital/god work).
- `project-dna/lumina/interfaces/export/aperture-hardening-mission-control.md` (post sub4: D2 Yellow (meta risk + "initial runtime_workers trading paths firewall (Paper/EOD Order sites now via bounded PaperTradeExecutor...)"; "Still full god (broader supervisor loops + other surfaces) + need follow-on for full decomp of either god"; highest "larger D2 decomp/firewall (new Plan Mode e.g. full supervisor decomp in runtime_workers or more meta surfaces per exact 05-31 D2)"; "Next Required Update Trigger": after D2 sub4 ... "Update MC+log+Guardian after each. Re-anchor to 2026-05-31 sources before god/risk/capital work").
- Sub4 evol log (2026-06-08-phase3-d2-subslice4-runtime-workers-trading-firewall.md): "Small"; "new bounded `PaperTradeExecutor` ... 3 direct sites ... (EOD ~301, paper open ~1219, paper close ~1299)"; "no other core changes (per plan: no supervisor/loop/daemon changes; no full PaperSimulator...)"; "Next (per MC + this log + 05-31 + protocol): Larger D2 (new Plan Mode mandatory): full(er) decomp/firewall of runtime_workers (e.g. supervisor loops, full PaperSimulator owning state + daemons, more pre_dream extraction)..."; "Per 2026-05-31 SPF-006 + D2 + MC explicit trigger"; "small measurable step"; "evidence-based per MC 'or runtime_workers per exact 05-31 D2' + explore".
- Fresh explore subagent this Plan Mode (id 019e8eb3-19fb-72c2-ae46-2a6c66c3a858): detailed survey post-sub4 (runtime_workers 1573 LOC; _paper_* 36-74 direct muts; EOD 232-344 now delegates executor 301; pre_dream 347-691 coupled via mutable app; supervisor_inner 841-1554 ~714 LOC procedural god mixing all; exact paper open 1209-1247 delegates executor 1218; open_pnl 1259-1271 direct app.open_pnl=; close 1273-1461 ~188 LOC with executor 1299 + post 1355+ appends/reflect/perf; dupe with pre_dream; scattered state 28+ files leak; no PaperSimulator yet; proposed narrow API try_open/check_close/get_open_pnl...; thin delegation sketches; risks/dupe/coupling; advances D2 + Phase2; "Still full god"; "small step on this god (PaperSimulator...) advances the D2 deliverable").
- `project-dna/lumina/core/constitution.md` (invariants 1/3/4/5/7).
- `project-dna/lumina/operating-system/self-improvement-protocol.md` + AGENTS.md + aperture-mission-control (SKILL.md): Plan Mode (this), hyp+pred, Small, evolution log + MC/agent-context/central updates, re-anchor, skills (constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract + aperture-mission-control).
- Skills (re-read this session): constitution-guard (7 rules), risk-safety-review (Score 9/10 + required ✅), test-scaffolding (given-when-then + monkeypatch + markers), event-bus-contract, aperture-mission-control (re-anchor + MC update + map to 05-31 D2 + Phase 2).
- Code (read-only + dedicated explore subagent this session + greps/reads): lumina_core/runtime_workers.py (post-sub4 state + 3 delegation sites for executor + D2 sub4 comments); lumina_core/engine/paper_trade_executor.py (full, for reuse); previous D2 (meta delegation pattern); broker/decision_lineage (lineage on Orders); tests (sub4 executor 5 green + prior runtime/paper).

**Classification**: **Small** (additive bounded PaperSimulator owning paper sim state + execution surface — the bulk of the trading god after sub4's Order construction firewall; thin delegation ~3-5 line calls from supervisor_inner at paper open/close/open_pnl/post sites; reuses PaperTradeExecutor + existing _paper_* + dream_snapshot + Economic/valuation/trade_workers; no behavior change on happy paths; no change to supervisor structure/loops/daemons/pre_dream/real/EOD main; SIM/paper friendly; independently testable; fully reversible; evidence-based per MC "full supervisor decomp in runtime_workers" + sub4 "Next: ... full PaperSimulator owning state" + 05-31 D2 "or runtime_workers").

**Impact on original plan**: Directly advances Phase 3 D2 (decomp/firewall of runtime_workers trading paths — the "or runtime_workers" god per 05-31 analysis/roadmap + MC "full supervisor decomp in runtime_workers" example + sub4 log "Next: ... full PaperSimulator owning state + daemons"; "changes inside it no longer require understanding the entire engine" for the paper sim state + execution surface (bulk after sub4 Order construction)). Supports Phase 2 deliv 2/4 (lineage/provenance on sim capital paths now centralized + encapsulated in bounded simulator + consistent with sub4 executor + broker paths). "At least one major" further progressed on runtime_workers (second god; sub4 Orders + this state/execution = meaningful decomp of trading paths god). Per 05-31 SPF-006 + D2 + MC explicit "continue D2 (new Plan Mode for next sub-slice e.g. ... full supervisor decomp in runtime_workers)" + sub4 "Next".

**Hypothesis (falsifiable)**: Creating bounded `PaperSimulator` (new focused class in lumina_core/engine/paper_simulator.py; narrow API try_open/check_close/get_open_pnl/get_sim_position/has_position + post; owns paper state/execution logic + relevant muts; reuses executor + _paper_* + valuation + Economic + trade_workers + PnlProvenance; "owner" via app=; docstring + Risk Safety Review (9/10) + constitution/skills blocks) + updating runtime_workers.py (import; thin delegation at paper open ~1209, open_pnl ~1259, close+post ~1273 blocks in supervisor_inner — ~3-5 line calls replacing logic; D2 sub5 hygiene comments citing 05-31 SPF-006 + D2 + MC "full supervisor decomp" + sub4 "PaperSimulator owning state" + "thin delegation"; minimal EOD if paper) + new tests (tests/engine/test_paper_simulator.py: 4-6 @pytest.mark.unit given-when-then + fail-closed/best-effort + supervisor mock integration + monkeypatch per scaffolding + sub4 style) + manual smoke (python -c with mocks + dream/ctx from proposal → try_open/check_close/get_* → executor with full lineage, state via _paper_*/p nl/peak, thin deleg, "MANUAL_SMOKE_SUB5_SUCCESS") + grep (PaperSimulator + thin calls in supervisor + "D2 sub-slice 5" + "PaperSimulator owning state" + MC "full supervisor decomp" / sub4 "PaperSimulator" comments) + relevant k-tests + Guardian will: (1) encapsulate paper sim state + execution in bounded PaperSimulator (no direct state muts on app/engine outside it in the trading paths surface of runtime_workers); (2) reduce the "understanding entire engine" surface for paper sim logic/state changes (now in simulator; supervisor_inner no longer mixes paper state/execution with price/RL/twin/swarm/gates/logging/oracle/EOD/real + dupe); (3) keep 100% backward compat + best-effort + no behavior change on happy (same qty/side/execution/ledger/PnL/peak/equity updates; same executor calls with full lineage; same open_pnl calc); (4) produce green tests (5/5 new + prior runtime/paper/D2) + manual (simulator calls + full lineage + no direct muts + thin deleg + success) + grep hits + Guardian clean (10.0/0 FATAL); (5) allow MC D2 comment to advance with "+ sub5: bounded PaperSimulator owning paper sim state + execution (thin deleg from supervisor_inner; uses PaperTradeExecutor; further decomp of runtime_workers trading god per 05-31 D2 + MC 'full supervisor decomp in runtime_workers' example + sub4 'Next: full PaperSimulator owning state')"; "at least one major" further progressed on runtime_workers; Phase 2 lineage centralized in simulator; (6) satisfy constitution-guard (1/3/4/5/7 + bounded no god + provenance), risk-safety (9/10 with paper state encapsulation + no optimistic + provenance + fail-closed/best-effort), test-scaffolding, event-bus-contract (if publish from simulator), aperture-mission-control; (7) no new bypasses, aperture 10.0, no whole-runtime/supervisor/pre_dream/EOD/real breakage. Prediction (measurable <5min): pytest on new simulator + k-runtime relevant green; manual shows + "MANUAL_SMOKE_SUB5_SUCCESS"; grep shows PaperSimulator + D2 comments; Guardian clean; new evol log + MC update with 05-31 + sub4 "Next" + MC "full supervisor decomp" cites + honesty; rollback trivial (revert simulator + thin calls + tests; restore blocks).

**What was executed (per the approved Plan Mode plan exactly)**:
- Re-anchored (aperture-mission-control + plan step 1): re-read immutable 05-31 analysis/roadmap + current MC (post sub4) + sub4 log + fresh dedicated explore subagent (id 019e8eb3-19fb-72c2-ae46-2a6c66c3a858; survey of post-sub4 concentrations, exact lines 1209/1259/1273, mut sites, dupe, proposed narrow API, delegation sketches, risks, D2 advancement mapping) + constitution + all 5 skills full (aperture-mission-control + constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract) + deep code (runtime_workers post-sub4 paper blocks + _paper_* + EOD + pre_dream + supervisor_inner; paper_trade_executor full for reuse; prior D2 meta delegation patterns).
- todo_write for the plan's concrete steps (D2 sub5 track).
- Core: created `lumina_core/engine/paper_simulator.py` (new bounded per plan "prefer new for bounded" + sub4 pattern + meta): PaperSimulator with __init__ (app/broker/container/executor/valuation), narrow API (try_open, check_close, get_open_pnl, get_sim_position, get_sim_entry_price, has_position, _post_close_actions), internal relocation of paper open/close + open_pnl + post (append/peak/trade_log/reflect/update_perf) + calls to executor + _paper_* + Economic + valuation; best-effort ctx from dream; docstring + Risk Safety Review (Score: 9/10) + Constitution Guard (1/3/4/5/7) + skills blocks per required format + 05-31/MC/sub4 cites; re-exports _paper_* for compat.
- Updated `lumina_core/runtime_workers.py`: added import; replaced paper open block (~1209) with thin delegation (simulator = PaperSimulator(...); res = simulator.try_open(...)); open_pnl (~1259) with thin (simulator.get_open_pnl); close+post (~1273) with thin (simulator.check_close which does hit + paper + handled post); added D2 sub5 hygiene comments citing 05-31 SPF-006 + Phase 3 D2 + MC "full supervisor decomp in runtime_workers" + sub4 "Next: full PaperSimulator owning state" + "thin delegation"; minimal scope patch for tail publish vars; no other changes (sub4 executor delegations + comments preserved; pre_dream/RL/twin/swarm/gates/EOD main/real paths untouched; supervisor structure/loops/daemons unchanged).
- Created `tests/engine/test_paper_simulator.py` (per plan + scaffolding): 5 tests (try_open with ctx/executor call + state; check_close hit + appends/history/peak/reflect/perf; get_open_pnl/state; best-effort fallback no broker/ctx; integration with supervisor mocks + dream ctx from proposal per sub4/Phase2); given-when-then in docstrings; @pytest.mark.unit; monkeypatch/mocks for app/broker/executor/valuation; fail-closed/best-effort explicit; "MANUAL_SMOKE_SUB5_SUCCESS" in smoke.
- Verifies (exact per plan section 5/7 + subagent): pytest new simulator 5/5 green (after mock/try fixes for reflect/swarm); broader k "runtime or paper or ... simulator" (183 passed including our; 10 pre-existing unrelated failures in broker_bridge/first_boot infinite_sim — no new regression from delegation); manual python -c smoke (OPEN_RES submit_ok True + CLOSE_HANDLED True + MANUAL_SMOKE_SUB5_SUCCESS); grep (9+ hits PaperSimulator + thin calls in supervisor + "D2 sub-slice 5" + "05-31 SPF-006" + "full supervisor decomp" + "PaperSimulator owning state" in runtime + simulator.py); runtime_workers import OK + sub4 preserved + sub5 comments; Guardian --report --d1-audits clean (exit 0, Structural 10.0, Aperture 10.0/10 GREEN, D3/D4 genuine note surfaced; no breakage).
- Forcing (post evidence per protocol + plan step 6): this new evol log (full format matching sub4 log + plan template: parents with verbatim 05-31/MC/sub4/explore quotes + subagent id, Small, hyp+pred, executed list, evidence (5/5 + manual + grep + Guardian + import), status impact with MC D2 note, next, rollback, *Per 2026-05-31 + MC + skills + protocol* + explicit maps to Phase 3 D2 + Phase 2 + SPF-006 + constitution); MC update (D2 row comment + last-updated + next trigger with brutal honesty + cites to this plan + 05-31 + "via bounded PaperSimulator"); append to central evolution-log.md; update agent-context.md D2 paragraph.
- No other core changes (per plan granularity + sub4 "no supervisor/loop/daemon changes; no full PaperSimulator" — this *is* the PaperSimulator small slice; no REAL capital change; no dupe resolution; no pre_dream extraction; best-effort inherited; paper remains lighter).

**Evidence** (from actual commands in this session):
- pytest: tests/engine/test_paper_simulator.py 5 passed (0.73s then 1.13s post fixes).
- Manual: OPEN_RES submit_ok True; CLOSE_HANDLED True; MANUAL_SMOKE_SUB5_SUCCESS.
- Grep: 9+ hits in runtime_workers + simulator.py for PaperSimulator + "D2 sub-slice 5" + "05-31 SPF-006" + "full supervisor decomp in runtime_workers" + "PaperSimulator owning state" + "thin delegation".
- Import/runtime smoke: RUNTIME_WORKERS_IMPORT_OK; HAS_PaperSimulator True; SUB4_PRESERVED True; SUB5_COMMENTS_PRESENT True.
- Guardian: exit 0; Structural Health 10.0/10; Aperture Integrity Score 10.0/10 GREEN; Phase 3 D3/D4 genuine note surfaced (no new warnings/breakage from sub5).
- Relevant k (background 166s): 183 passed (incl new simulator + core paper/runtime); 10 pre-existing unrelated (broker_bridge lineage tests + first_boot infinite_sim) — no regression attributable to thin delegation or PaperSimulator.
- Repro (per plan): `python -m pytest tests/engine/test_paper_simulator.py -q --tb=short`; the manual python -c smoke; `python -m pytest -q --tb=no -k "runtime or paper or ... simulator" | Select -Last 5`; Guardian; grep; python -c "import lumina_core.runtime_workers as rw; ...".
- Explore subagent output (id 019e8eb3-...): verbatim lines, mut sites, delegation sketches, API, risks, D2 mapping used to validate plan + impl.

**Status impact**:
- D2: Yellow (continued/strengthened; "+ sub5: bounded PaperSimulator owning paper sim state + execution (thin deleg from supervisor_inner; uses PaperTradeExecutor; further decomp of runtime_workers trading god per 05-31 D2 + MC 'full supervisor decomp in runtime_workers' example + sub4 'Next: full PaperSimulator owning state')"; "at least one major" further progressed on runtime_workers (second god per 05-31 + SPF-006)).
- Phase 2 lineage: paper sim state/execution + post (trade_log/perf/peak) now centralized/encapsulated in bounded simulator (consistent with sub4 full lineage on Orders via executor; supports D1 audit + hash chain on sim capital).
- MC updated with the above + highest/next (still "larger D2 decomp/firewall (new Plan Mode e.g. full supervisor decomp in runtime_workers or more meta surfaces...) or D3...").
- New evol log (this) + central append + agent-context refresh.
- Skills applied/documented (constitution-guard 1/3/4/5/7 + bounded no god + provenance, risk-safety 9/10 with paper state encapsulation + no optimistic + provenance, test-scaffolding, aperture-mission-control).
- No new bypasses, aperture 10.0/0 FATAL, new tests green + prior relevant, no happy regression, SIM/paper friendly, reversible (revert simulator + 3 thin call sites + tests; restore original blocks; no state/capital impact).
- Parent 2026-05-31 D2 + SPF-006 + Phase 2 deliv 2/4 advanced by one small measurable bounded sub-slice on the paper state/execution surface inside the second god (complements sub4 Orders + meta risk sub1-3); "at least one major" (meta + runtime_workers) further progressed per exact deliverable.

**Next (per MC + this log + 05-31 + protocol + subagent)**:
- Larger D2 (new Plan Mode mandatory): full(er) decomp/firewall of runtime_workers (e.g. EOD closer extraction, supervisor phases into state machine, pre_dream narrow API, dupe resolution with pre_dream, more surfaces) or more meta surfaces (orchestration/AB/promotion/bloat removal) per exact 05-31 D2 + MC "full supervisor decomp in runtime_workers" + sub4 "Next".
- Or parallel: more D3 (Guardian self-score vs aperture contracts, auto D1 on genuine incl paper paths, fail on missing lineage), D5 close remaining, D6, Phase 2 full Pydantic subscribers (no raw dicts).
- Always: re-anchor 05-31 + MC first; use skills; update forcing after increment; small additive; Guardian daily; 0 bypass/10.0.
- Reproduce sub5: `python -m pytest tests/engine/test_paper_simulator.py -q --tb=short`; the manual python -c from plan/verify; `python scripts/dna_guardian/validate_dna.py --report --d1-audits`; grep + import smoke.

**Rollback**: Revert the paper_simulator.py + import + 3 thin delegation sites in runtime_workers (restore original paper open/close/open_pnl/post blocks + direct _paper_*/app/engine muts); revert new tests; revert MC/agent-context/central + this log (or mark superseded). No state/data impact (logic relocated + encapsulated; execution/ledger/PnL/peak/equity identical; lineage on Orders from sub4 unchanged; sim/paper by design). Callers (supervisor, EOD if paper) unaffected on happy (same submit/execution/ledger/PnL). Per 05-31 + prior D2 logs + MC + protocol.

**Evidence Bundle Map to 2026-05-31 Deliverables**:
- Phase 3 D2: firewalling of runtime_workers trading paths (second god per analysis + "or runtime_workers" in deliverable) — paper sim state + execution (bulk after sub4 Orders) now in bounded PaperSimulator ("changes inside no longer require understanding the entire engine").
- Phase 2 deliv 2/4: full lineage/provenance on sim capital paths (Orders via sub4 executor + state/execution/post now centralized in simulator; dream ctx flows).
- Cross: small step, evidence (5/5 tests + manual + grep + Guardian + import + MC/log), forcing updated with honesty + 05-31 cites, transparent (simulator + thin calls explicit), no drift, SIM/paper, 0 bypass/10.0 preserved.
- Per explore subagent + plan + sub4: "small step compounds for full god decomp in follow-ons".

This sub-slice was developed after Plan Mode (user "enter plan mode and proceed with the next step" → re-anchor + explore subagent + plan.md written/polished + exit + approved), followed the approved plan (section 4 concrete strategy + verification + skills + forcing) exactly, produced measurable evidence, maintained perfect strategic visibility + brutal honesty against the 2026-05-31 north star and current MC.

*Per the 2026-05-31 Elon first-principles analysis + 90-day roadmap + permanent aperture-mission-control skill + Recursive Self-Improvement Protocol + AGENTS.md + constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract skills.*

**Evidence Bundle Map to 2026-05-31 Deliverables** (repeated for audit): Phase 3 D2 + Phase 2 deliv 2/4 + SPF-006 + constitution 1/3/4/5/7 + skills + protocol as above.

(Continues D2 track: sub1-3 meta risk mutation (typed apply + helper creation injection) → sub4 runtime_workers Order construction (bounded executor) → sub5 runtime_workers paper state/execution (bounded simulator + thin deleg). "At least one major" (meta + runtime) progressed; Phase 2 lineage on all orders (incl sim capital) advanced. Next requires fresh Plan Mode for larger god work per MC/05-31.)