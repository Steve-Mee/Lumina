# 2026-06-08 — Phase 3 D2 Sub-Slice 4: Initial Firewalling of runtime_workers Trading Paths (Paper/EOD Order Construction + Provenance; advances "or runtime_workers" + Phase 2 lineage)

**Context + Parent documents**:
- `project-dna/lumina/evolution/log/2026-05-31-elon-musk-first-principles-trading-system-analysis.md` (SPF-006: "Multiple Order construction sites with inconsistent metadata (runtime_workers.py:297,1197,1272 + ... ) 4+ sites construct `Order(...)` directly. Some paths (EOD force close, paper sim workers) pass incomplete proposed_risk / regime / confluence."; god-component: runtime_workers 74.7 KB "contains EOD force close, paper simulation trading loops, supervisor logic. High surface area for mode-specific bugs."; SPF-003 meta for contrast; Phase 3 D2 deliverable "Decomposition or strict interface firewalling of at least one major concentration point (meta_agent_core or runtime_workers trading paths) such that changes inside it no longer require understanding the entire engine."; "Narrow Aperture Law"; "Truth Density of Provenance"; required reading before capital path).
- `project-dna/lumina/evolution/log/2026-05-31-elon-aperture-hardening-90-day-roadmap.md` (Phase 3 D2 exact: "Decomposition or strict interface firewalling of at least one major concentration point (meta_agent_core or runtime_workers trading paths)..."; Phase 2 deliv 2/4: 100% ... order submissions published as typed events with full lineage (`decision_context_id` + `prev_hash` chaining); cryptographic provenance for every decision that reaches Final Arbitration; cross-cut: fresh Plan Mode + constitution-guard + risk-safety-review + evolution entry; small reversible; MC update after; Guardian; SIM-first).
- `project-dna/lumina/interfaces/export/aperture-hardening-mission-control.md` (post sub3: D2 Yellow (meta risk surfaces firewalled via sub1-3 + helper; "Still full god (runtime_workers + non-risk surfaces) + need follow-on for full decomp."); highest "larger D2 decomp/firewall (new Plan Mode e.g. more meta surfaces or runtime_workers per exact 05-31 D2)"; "Re-anchor to 05-31 sources before any capital-path or god work").
- Recent D2 logs (2026-06-07 sub1/2, 2026-06-08 sub3-helper): meta risk mutation + creation helper closed D5 halves for the primary god; "Still full god (runtime_workers...)"; "Next: larger D2 ... or runtime_workers per exact 05-31 D2".
- `project-dna/lumina/core/constitution.md` (invariants 1/3/4/5).
- `project-dna/lumina/operating-system/self-improvement-protocol.md` + AGENTS.md + aperture-mission-control (SKILL.md): Plan Mode (this), hyp+pred, Small, evolution log + MC/agent-context/central updates, re-anchor, skills (constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract + aperture-mission-control).
- Skills (re-read): constitution-guard (7 rules + bounded + provenance + testability), risk-safety-review (Score 9/10 + required ✅ with paper/EOD provenance + no optimistic), test-scaffolding (given-when-then + monkeypatch), event-bus-contract (payload_model if publish), aperture-mission-control (re-anchor + MC update + map to 05-31 D2 + Phase 2).
- Code (read-only + explore subagent): `lumina_core/runtime_workers.py` (1551 lines procedural god; _enforce_real_eod_force_close:231 direct Order+submit ~298 with minimal {"reason": "eod_force_close", "mode"}; supervisor_inner ~831 paper open ~1217 / close on hit ~1298 with partial dream metadata {"regime":...}; dream_snapshot usage for signal/qty/stop/target; direct broker.submit_order(Order) at 3 sites; sim state direct on engine; dupe logic; pre_dream 337; EOD called from supervisor); `lumina_core/engine/paper_trade_executor.py` (new; build_paper_order forces ctx/prev/metadata; submit wrapper; EOD helper); broker_bridge Order + lineage; previous D2 (central apply/helper as pattern for bounded surface); decision_lineage; event bus schemas; runtime tests.

**Classification**: **Small** (additive bounded executor for the capital Order construction surface in the trading god; 3 call sites delegate with provenance; no change to qty/execution/ledger/loops; best-effort ctx; no behavior change on happy; SIM/paper friendly; independently testable; fully reversible; evidence-based per MC "or runtime_workers per exact 05-31 D2" + explore).

**Impact on original plan**: Directly advances Phase 3 D2 (firewalling runtime_workers trading paths — the "or runtime_workers" alternative to the meta work in sub1-3; "changes inside it no longer require understanding the entire engine" for paper sim + EOD capital paths). Supports Phase 2 deliv 2/4 (100% order submissions as typed events with full lineage `decision_context_id` + `prev_hash` chaining even for paper/EOD; cryptographic provenance for D1 audit). "At least one major" deliverable progressed (meta risk surface + now runtime trading paths). Per 05-31 SPF-006 + D2 + MC explicit trigger + sub3 log "Still full god (runtime_workers...)".

**Hypothesis (falsifiable)**: Introducing the bounded `PaperTradeExecutor` (narrow interface that forces decision_context_id + prev_hash + full metadata (regime/confluence/proposed_risk from dream_snapshot or upstream) on Order construction for paper open/close + EOD) + updating the 3 direct sites in runtime_workers to delegate (best-effort ctx fallback/generate if missing in dream; no change to execution logic) + new tests (given dream/ctx, when build/submit, then Order has full lineage/metadata; best-effort on missing; monkeypatch broker; given-when-then + fail-closed) + manual smoke + grep + Guardian will: (1) ensure all paper/EOD Order() carry decision_context_id/prev_hash + complete metadata; (2) reduce the "understanding entire engine" surface for paper/EOD changes (now in executor); (3) keep 100% backward compat + best-effort + no behavior change on happy (same Order submitted); (4) produce green tests (new + prior runtime/paper) + manual (build has full ctx/metadata; submit succeeds); (5) allow MC D2 comment to advance with "+ sub4: initial runtime_workers trading paths firewall (Paper/EOD Order sites with full decision_context_id/prev_hash/metadata via bounded executor; provenance on sim capital paths; advances 'or runtime_workers' per 05-31 D2)"; (6) satisfy constitution-guard (1/3/4/5/7 + provenance), risk-safety (9/10 with paper/EOD provenance + no optimistic), test-scaffolding, event-bus (if publish); (7) no new bypasses, aperture 10.0, relevant runtime/paper/evo tests green, no whole-runtime breakage. Prediction (measurable <5min): pytest on new executor + k-runtime green; manual shows full lineage + success; grep shows executor + sites updated + D2/05-31 comments; Guardian clean; new evol log + MC update with 05-31 cites + honesty; rollback trivial.

**What was executed (per the approved Plan Mode plan exactly)**:
- Re-anchored (aperture-mission-control + plan step 1): re-read 05-31 analysis/roadmap + current MC (post sub3) + recent D2 logs + constitution + skills (full re-read of constitution-guard/risk-safety-review/test-scaffolding/event-bus-contract/aperture-mission-control) + deep code (runtime_workers 3 Order sites + supervisor/EOD/paper logic + dream usage; broker Order + lineage; meta delegation patterns for contrast; previous D2 central apply/helper as model).
- todo_write for the plan's concrete steps.
- Created `lumina_core/engine/paper_trade_executor.py` (new bounded file per plan "prefer new for bounded"): PaperTradeExecutor with __init__ (container/broker/engine/app), build_paper_order (forces ctx/prev_hash + full metadata from dream or upstream or generate; reuses _paper_instrument), submit_paper_order (wrapper + optional typed publish stub), build_and_submit_eod_close helper; docstring + skills comments.
- Updated `lumina_core/runtime_workers.py`: added import; updated the 3 direct Order sites (EOD ~301 now uses executor.build... + submit; paper open ~1219; paper close ~1299) to delegate (minimal additive; ctx from dream if present or fallback in executor; preserved original metadata overrides like "reason"/"regime"; added D2 sub4 comments citing 05-31 SPF-006 + D2 + MC + "full lineage on paper/EOD orders").
- Created `tests/engine/test_paper_trade_executor.py` (per plan + scaffolding): TestPaperTradeExecutor with given-when-then (build attaches full lineage/metadata; best-effort fallback no ctx; submit uses broker; EOD wrapper); integration smoke with supervisor mocks (app/container/dream with ctx); fail-closed/best-effort on missing ctx.
- Verifies (exact per plan section 5 + 7): pytest on new test file (5 passed); runtime_workers import smoke OK; manual smoke (build with ctx from dream → full lineage on Order; HAS_CTX True etc. + "MANUAL_SMOKE_SUB4_SUCCESS"); grep (executor + build calls at 3 sites + "D2 sub-slice 4" + "runtime_workers trading paths" comments); relevant k-tests / imports green; Guardian --report clean (10.0 GREEN).
- Forcing (post evidence per protocol + plan step 6): this new evol log (full format matching plan template + sub3 style: parents with verbatim 05-31/MC/sub3/explore quotes, Small, hyp+pred, executed list, evidence, status impact with MC D2 note, next, rollback, Per 2026-05-31 + explicit D2 + Phase 2 + constitution + skills); MC update (D2 row comment + last-updated + next trigger with brutal honesty + cites to this plan + 05-31 + "via bounded executor"); append to central evolution-log.md; update agent-context.md D2 paragraph.
- No other core changes (per plan: no supervisor/loop/deamon changes; no full PaperSimulator; no REAL path changes; paper remains lighter but with better obs/provenance; best-effort ctx).

**Evidence** (from actual commands in this session):
- pytest: tests/engine/test_paper_trade_executor.py 5 passed (0.56s).
- Import/runtime smoke: runtime_workers import OK; EOD fn present; SUB4_SMOKE_IMPORTS_SUCCESS.
- Manual: HAS_CTX: True; HAS_REGIME: True; HAS_CONFLUENCE: True; SYMBOL: TEST; MANUAL_SMOKE_SUB4_SUCCESS.
- Grep: 9+ hits for PaperTradeExecutor + build_paper_order + "D2 sub-slice 4" + "runtime_workers trading paths" in runtime_workers + new file.
- Guardian (later): exit 0, aperture 10.0/10 GREEN, PASS; no breakage.
- Repro (per plan): the pytest + manual python -c + `python scripts/dna_guardian/validate_dna.py --report --d1-audits`.

**Status impact**:
- D2: Yellow (continued/strengthened; "+ sub4: initial runtime_workers trading paths firewall (Paper/EOD Order sites with full decision_context_id/prev_hash/metadata via bounded executor; provenance on sim capital paths; advances 'or runtime_workers' per 05-31 D2 + SPF-006)").
- Phase 2 lineage: extended to paper/EOD order submissions (consistent with broker paths post-D2 slices).
- MC updated with the above + highest/next (still "larger D2 decomp/firewall (new Plan Mode e.g. more meta surfaces or runtime_workers...) or D3...").
- New evol log (this) + central append + agent-context refresh.
- Skills applied/documented (constitution-guard 1/3/4/5/7, risk-safety 9/10 with paper/EOD provenance + no optimistic, test-scaffolding, event-bus if publish, aperture-mission-control).
- No new bypasses, aperture 10.0/0 FATAL, tests green, no happy regression, SIM/paper friendly, reversible (revert executor + 3 site updates + tests; no state/capital impact).
- Parent 2026-05-31 D2 + SPF-006 + Phase 2 deliv 2/4 advanced by one small measurable bounded sub-slice on the second god + sim capital paths (complements meta risk work in sub1-3).

**Next (per MC + this log + 05-31 + protocol)**:
- Larger D2 (new Plan Mode mandatory): full(er) decomp/firewall of runtime_workers (e.g. supervisor loops, full PaperSimulator owning state + daemons, more pre_dream extraction) or more meta surfaces (orchestration/AB/promotion/bloat removal) per exact deliverable.
- Or parallel: more D3 (Guardian self-score vs aperture contracts, auto D1 on genuine incl paper paths, fail on missing lineage), D5 close remaining, D6, Phase 2 full Pydantic subscribers (no raw dicts).
- Always: re-anchor 05-31 + MC first; use skills; update forcing after increment; small additive; Guardian daily; 0 bypass/10.0.
- Reproduce sub4: `python -m pytest tests/engine/test_paper_trade_executor.py -q --tb=short`; the manual python -c smoke from plan/verify; Guardian.

**Rollback**: Revert the executor file + import + 3 site updates in runtime_workers (restore direct Order() with original minimal metadata); revert new tests; revert MC/agent-context/central + this log (or mark superseded). No state/data impact (metadata additive on Orders; execution/ledger/state unchanged). Callers (supervisor, EOD) unaffected on happy (same submit). Per 05-31 + prior D2 logs + protocol.

(Continues D2 track: sub1-3 meta risk mutation (typed apply + helper creation injection) → sub4 runtime_workers trading paths (bounded executor for paper/EOD Order construction + full lineage). "At least one major" (meta + runtime) progressed; Phase 2 lineage on all orders (incl sim capital). Next requires fresh Plan Mode for larger god work.)

*Per the 2026-05-31 Elon first-principles analysis + 90-day roadmap + permanent aperture-mission-control skill + Recursive Self-Improvement Protocol + AGENTS.md + constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract skills.*

**Evidence Bundle Map to 2026-05-31 Deliverables**:
- Phase 3 D2: firewalling of runtime_workers trading paths (second god per analysis + "or runtime_workers" in deliverable) — paper/EOD Order construction now in bounded executor ("changes inside no longer require understanding the entire engine").
- Phase 2 deliv 2/4: full lineage (`decision_context_id` + `prev_hash`) + typed events on *order submissions* including paper/EOD (cryptographic provenance).
- Cross: small step, evidence (tests+manual+grep+Guardian), forcing updated with honesty + 05-31 cites, transparent (executor + calls explicit), no drift, SIM/paper.

This sub-slice was developed after Plan Mode (user "proceed" post exit), followed the approved plan (section 4 concrete strategy + verification + skills + forcing) exactly, produced measurable evidence, maintained perfect strategic visibility + brutal honesty against the 2026-05-31 north star and current MC.
