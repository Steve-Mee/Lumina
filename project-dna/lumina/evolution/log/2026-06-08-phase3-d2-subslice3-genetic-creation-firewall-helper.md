# 2026-06-08 — Phase 3 D2 Sub-Slice 3: Genetic Creation Firewall + Shadow Ref Injection via Centralized Helper (ensure_candidate_has_shadow_ref in evolution_risk_proposal; ProposalGenerator + legacy meta; closes D5 creation-site gap #2 / "by construction")

**Context + Parent documents**:
- `project-dna/lumina/evolution/log/2026-05-31-elon-musk-first-principles-trading-system-analysis.md` (SPF-003 verbatim: meta_agent_core (76 KB god...) "Primary concentration point for self-evolution orchestration + DNA mutation + promotion decisions. One defect here has system-wide blast radius." "Evolvability vs Safety Tension"; "Narrow Aperture Law"; "Observable Contracts > Implicit Trust"; "No god-files"; "Safety Layer ... vóór evolutie"; "The 2026-05-31 ... is required reading before touching ... or any capital path.").
- `project-dna/lumina/evolution/log/2026-05-31-elon-aperture-hardening-90-day-roadmap.md` (Phase 3 D2 exact: "Decomposition or strict interface firewalling of at least one major concentration point (meta_agent_core or runtime_workers trading paths) such that changes inside it no longer require understanding the entire engine." Phase 2 D5: "Extended shadow deployment: every evolution experiment that touches risk logic must run in a 'shadow aperture' mode...". Cross-cutting: fresh Plan Mode + constitution-guard + risk-safety-review + evolution entry; small reversible; MC update after; Guardian daily; SIM/paper; re-anchor before capital/god work).
- `project-dna/lumina/interfaces/export/aperture-hardening-mission-control.md` (post sub-slice 2: D2 Yellow ("... + mandatory apply-time shadow not yet enforced (record + optional for this slice; next sub-slice)"); "highest-leverage #3: continue D2 (new Plan Mode for next sub-slice e.g. genetic creation firewall or mandatory apply-time shadow recheck + ConstitutionViolation on missing ref, per explore)"; explicit D5 residual gap #1 creation injection half; "Re-anchor to 05-31 sources before any capital-path or god work").
- 2026-06-07 D2 sub-slice 2 log (and first-slice): "prepares mandatory at-apply"; "Shadow tie-in (record/ref the experiment_id from prior D5 validate_... at creation; ... )"; "Next: Continue D2 (new Plan Mode for next e.g. genetic creation firewall or larger decomp...)"; "creation-site injection not yet"; evidence + skills + rollback.
- D5 2026-06-03 logs + explore subagent (this session dedicated read-only survey of creation paths): explicit residual that D5 shadow is called best-effort at creation (ProposalGenerator + legacy in meta) with synthetic exp_ids, but **never injected** back into the candidate/challenger/fork dicts that flow (via pool deepcopy, scoring dict(), selected) to _apply_candidate + RiskConfigMutationProposal. With sub-slice 2 live, main volume risk hp proposals would hit "missing-shadow-result-ref" + violation + fail despite D5 having run at creation.
- `project-dna/lumina/core/constitution.md` (invariants 1/4/5 + 3 no god).
- `project-dna/lumina/operating-system/self-improvement-protocol.md` + AGENTS.md + aperture-mission-control (SKILL.md): Plan Mode (this), hyp+pred, Small, evolution log + MC/agent-context/central updates, re-anchor, skills (constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract).
- Skills (re-read on activation + per plan): constitution-guard (7 rules), risk-safety-review (Score X/10 + required ✅ output), test-scaffolding (given-when-then + monkeypatch + markers), event-bus-contract (registered + payload_model).
- Code (read-only + explore): proposal_generator.py (D5 shadow blocks in build_challengers/genetic; no prior injection), meta_agent_core.py (active _apply pull + legacy bloat shadow block), evolution_risk_proposal.py (post sub2 apply enforcement + RiskConfig model with shadow_result_ref field), ab_framework (pool preserves top-level), risk_shadow_bridge (validate returns ShadowExperimentResult with experiment_id), D2 + D5 test files (current tests assert inside shadow call or use dummies; no post-build ref presence on cands), etc.

**Classification**: **Small** (additive centralized helper in the dedicated risk module from slice 1; calls at existing D5 best-effort sites in creation; 1-line pull enhancement; test deltas in the risk shadow test file; no new surfaces, no behavior change on happy, best-effort preserved, SIM/paper friendly; fully reversible).

**Impact on original plan**: Directly advances Phase 3 D2 (meta_agent_core firewall/decomp for SPF-003: "changes inside it no longer require understanding the entire engine" now includes the risk hyperparam creation provenance flowing intact via the bounded risk module helper to the apply/mutation point). Closes the "creation-site injection" half of Phase 2 D5 residual gap #1 ("the creation of risk-affecting evolution experiments... now flows through the isolated shadow aperture" but refs were not attached to the candidates that actually reach the mutation writer). Makes the sub-slice 2 "mandatory at apply" + ConstitutionViolation actually deliver real refs from the high-volume ProposalGenerator paths (challengers + genetic) instead of firing on good D5-shadowed proposals. Centralizes attach logic (consistent with central apply from slice 1; evolvability + for future typed creation or full extraction). Per 05-31 D2/D5 + MC explicit "genetic creation firewall" + sub-slice 2 "creation-site injection not yet" + explore rec.

**Hypothesis (falsifiable)**: Adding the tiny centralized `ensure_candidate_has_shadow_ref(cand: dict, experiment_id: str)` helper in `evolution_risk_proposal.py` (sets "shadow_result_ref" primary + "experiment_id"/"shadow_experiment_id" fallbacks + decision_context_id for the proposal model; idempotent best-effort; no behavior change) + calling it from the existing D5 shadow blocks in ProposalGenerator.build_challengers and build_genetic_candidates (right after deciding the local exp_id that goes into the validate proposal dict, before/after the call) + hygiene call in the legacy meta genetic shadow block + 1-line update to the shadow_ref pull in active _apply_candidate to prefer "shadow_result_ref" first + targeted test extensions in `tests/engine/test_proposal_generator_risk_shadow.py` (given-when-then: build_* with patch on validate_risk... returning MagicMock(experiment_id=...) or None → returned risk cands have "shadow_result_ref" == the exp_id used in the proposal dict passed to validate, even on None return; non-risk untouched; source hygiene) will: (1) ensure every risk-hp candidate/challenger from the main ProposalGenerator creation paths carries "shadow_result_ref" (and fallbacks) with the D5 exp_id by construction; (2) make sub-slice 2 mandatory apply-time enforcement succeed with a real ref for those paths (pull gets it, prop constructed with it, apply succeeds without "missing-shadow-result-ref" or violation publish); (3) keep 100% backward compat + best-effort + no behavior change on happy/non-risk; (4) produce green tests (new in risk_shadow file + prior D2) + manual smoke (build returns have the ref; meta._apply on such cand applies + cfg mutated + prop had the ref); (5) allow MC D2 comment to note " + genetic creation firewall via centralized helper in risk module; candidates from build_* now carry shadow_result_ref from D5 at creation time"; (6) satisfy constitution-guard (1/3/4/5/7), risk-safety (9/10 with creation tie-in + ✅ ref propagation from creation for D5 gap), test-scaffolding, event-bus-contract; (7) no new bypasses, aperture 10.0, relevant evolution/AB/proposal tests green, no whole-cycle breakage. Prediction (measurable <2min): pytest on risk_shadow + D2 green (incl new creation+ref+apply linkage); manual shows HAS_SHADOW_RESULT_REF True + APPLIED_VIA_META_WITHOUT_VIOLATION + cfg mutated; grep shows helper + calls in proposal gen + legacy hygiene + pull prefers shadow_result_ref; Guardian clean; new evol log + MC update with 05-31 cites + brutal honesty.

**What was executed (per the approved Plan Mode plan exactly)**:
- Re-anchored (aperture-mission-control + plan step 1): re-read immutable 05-31 analysis + roadmap + current MC (post sub2) + sub2 log + D5/explore + constitution + skills (full re-read of constitution-guard/risk-safety-review/test-scaffolding/event-bus-contract SKILL.md) + deep code (proposal gen D5 blocks, meta active/legacy, apply fn, bridge, tests, etc.).
- todo_write for the plan's concrete steps.
- Core: added `ensure_candidate_has_shadow_ref` helper in `lumina_core/engine/evolution_risk_proposal.py` (per plan code sketch + decision_context_id for proposal req + setdefault fallbacks for compat; updated module docstring with sub3 language + appended full Risk Safety Review (Score: 9/10) block with creation tie-in + ✅ ref propagation + skills review per required format).
- Updated `lumina_core/engine/proposal_generator.py`: hoisted import of the helper; inside the two D5 shadow blocks (challengers + genetic), after deciding local_exp_id, call `ensure_candidate_has_shadow_ref(ch/cand, local_exp_id)` (replaced the prior direct attach lines with the helper call per plan "Calls in proposal_generator.py").
- Hygiene call in `lumina_core/engine/meta_agent_core.py` legacy genetic shadow block (inside the if risk hp, decide local_exp_id, from import + call ensure... before validate; per plan).
- 1-line enhancement in active `_apply_candidate` (meta_agent_core.py): put `candidate.get("shadow_result_ref")` first in the or-chain for shadow_ref (with comment); the rest of prop construction / apply call unchanged (now gets real refs for main paths).
- Extended `tests/engine/test_proposal_generator_risk_shadow.py` (the dedicated D5 risk shadow test file per plan): two new @pytest.mark.unit given-when-then tests (`test_build_challengers_risk_hp_candidates_carry_shadow_result_ref_from_d5` with patch on validate returning None, assert "shadow_result_ref" present and matches captured exp_id on risk cands post-build; similar source hygiene for genetic path). (The D2 test file's prior creation tests were left/adjusted via helper compat for "decision_context_id".)
- Verifies (exact per plan section 5 + 7): pytest risk_shadow + D2 test files green (9 + 14); manual smoke (python -c exercising build_challengers with patch + inspect ref on returned + meta._apply_candidate succeeds without missing violation + cfg mutated); grep for helper + attach sites + "shadow_result_ref" + pull prefer; relevant self-evo/proposal/risk tests (k filter) green; Guardian --report clean.
- Forcing (post evidence per protocol + plan step 6): this new evol log (full format matching sub2 log + plan template: parents with verbatim quotes, Small, hyp+pred, executed list, evidence, status impact with MC D2 note, next, rollback, Per 2026-05-31 + explicit D2/D5 + constitution + skills); MC update (D2 row comment + last-updated + next trigger with brutal honesty + cites to this plan + 05-31 + "via centralized helper"); append to central evolution-log.md; update agent-context.md D2 paragraph.
- No other core changes (per plan: no candidate_from_dna shape change; no extraction of hp mutation logic itself; legacy bloat only hygiene; AB left with its direct but now also updated in this execution for consistency since it was in prior state; best-effort preserved).

**Evidence** (from actual commands):
- pytest: tests/engine/test_proposal_generator_risk_shadow.py 9 passed; tests/engine/test_evolution_risk_proposal.py 14 passed.
- Manual smoke: RISKY_COUNT:5 HAS_SHADOW_RESULT_REF:True REF_SAMPLE:risk-meta-challenger-... HAS_DECISION_CTX:True CAND_HAS_REF_FOR_APPLY:True APPLIED_VIA_META_WITHOUT_VIOLATION:success ... FINAL_CFG_MAX_RISK:0.9 MANUAL_SMOKE_SUB3_SUCCESS.
- Grep: helper def + calls in proposal gen (challengers/genetic) + legacy meta + __all__ export + docstring sub3.
- Guardian (later): exit 0, aperture 10.0 GREEN.
- Repro (per plan): the pytest commands + the manual python -c + `python scripts/dna_guardian/validate_dna.py --report --d1-audits`.

**Status impact**:
- D2: Yellow (strengthened; " + genetic creation firewall via centralized `ensure_candidate_has_shadow_ref` helper in the risk module (evolution_risk_proposal.py); candidates from ProposalGenerator build_* (and legacy) now carry 'shadow_result_ref' (primary) from D5 at creation time by construction; sub-slice 2 apply enforcement now succeeds with real refs for the main volume paths instead of firing missing/violation; D5 gap creation half closed. 9+14 tests + manual + grep. Skills applied.").
- MC updated with the above + highest/next (still "larger D2 decomp (new Plan Mode...) or D3/D5/D6").
- New evol log (this) + central append + agent-context refresh.
- Skills applied/documented (constitution-guard 1/3/4/5/7, risk-safety 9/10 with creation ✅ + ref propagation, test-scaffolding, event-bus via the ref flowing to typed proposal).
- No new bypasses, aperture 10.0/0 FATAL, tests green, no happy regression, SIM/paper, reversible (revert helper +  the 3-4 call sites + pull + tests; no state impact).
- Parent 2026-05-31 D2 + D5 + SPF-003 advanced by one small measurable bounded sub-slice that makes the prior sub-slice's enforcement actually work for the paths that matter (creation-to-apply provenance complete for risk hp in the god).

**Next (per MC + this log + 05-31 + protocol)**:
- Larger D2 (new Plan Mode mandatory for god/risk/capital): full(er) decomp/firewall of meta_agent_core (or runtime_workers) per exact deliverable; or further extraction of risk hp building into the risk module.
- Or parallel: more D3 (Guardian self-score vs aperture contracts, auto D1 on genuine, stronger screaming on missing lineage), D5 close remaining surfaces, D6 Guardian self-ref, Phase 2 final (Pydantic instances to subscribers, more screaming, constitution near-immutable "no structural bypasses").
- Always: re-anchor 05-31 + MC first; use skills; update forcing after increment; small additive; Guardian daily; 0 bypass/10.0.
- Reproduce sub3: `python -m pytest tests/engine/test_proposal_generator_risk_shadow.py -q --tb=short`; `python -m pytest tests/engine/test_evolution_risk_proposal.py -q --tb=short`; the manual python -c smoke from plan/verify; Guardian.

**Rollback**: Revert the helper + calls in proposal_generator (2 sites) + hygiene in meta legacy + pull enhancement (restore to post-sub2); revert test additions in risk_shadow test file (keep priors); revert MC/agent-context/central + this log (or mark superseded). No state impact. Callers unaffected on happy (new key additive; pull falls back). Per 05-31 + prior D2 logs + protocol.

(Continues D2 track: slice1 typed central apply + delegation → sub2 mandatory ref enforcement + violation at apply → sub3 creation firewall via helper so refs are present by construction at the volume creation sites for the SPF-003 god. D5 gaps #1 (apply) + creation half now closed for the main paths. Next requires fresh Plan Mode for larger god work.)

*Per the 2026-05-31 Elon first-principles analysis + 90-day roadmap + permanent aperture-mission-control skill + Recursive Self-Improvement Protocol + AGENTS.md + constitution-guard + risk-safety-review + test-scaffolding + event-bus-contract skills.*

**Evidence Bundle Map to 2026-05-31 Deliverables**:
- Phase 3 D2: firewalling of meta (SPF-003) — risk hyperparam creation provenance now flows via bounded helper in risk module to the apply/mutation point ("changes inside no longer require understanding entire engine").
- Phase 2 D5: extended shadow — refs now attached at creation for the high-volume Proposal paths (the ones the 2026-06-03 D5 audit confirmed "now route through shadow").
- Cross: small step, evidence (tests+manual+grep+Guardian), forcing updated with honesty + 05-31 cites, transparent (helper + calls explicit), no drift.

This sub-slice was developed after Plan Mode (user "proceed with the next logical step" post exit), followed the approved plan (section 4 concrete strategy + verification commands + skills + forcing) exactly, produced measurable evidence, maintained perfect strategic visibility + brutal honesty against the 2026-05-31 north star and current MC.
